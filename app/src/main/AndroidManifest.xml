<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.meapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MeApp">

        <activity
            android:name="com.example.meapp.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.meapp.BiodataActivity"
            android:parentActivityName=".MainActivity"
            android:label="Biodata"/>
        <activity android:name="com.example.meapp.DaftarHewanActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name="com.example.meapp.ProfilActivity"
            android:parentActivityName=".DaftarHewanActivity"/>
    </application>

</manifest>